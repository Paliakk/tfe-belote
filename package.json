{
  "name": "belote-monorepo",
  "private": true,
  "scripts": {
    "install": "npm --prefix frontend install && npm --prefix backend install",
    "dev": "concurrently -n FRONT,BACK -c green,blue \"npm run dev --prefix frontend\" \"npm run start:dev --prefix backend\"",
    "build": "npm run install && npm run build-only",
    "build-only": "npm run build --prefix frontend && npm run build --prefix backend",
    "start": "npm run start:prod --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}